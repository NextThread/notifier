name: Calendar Reminder

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  reminder:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install dependencies
        run: pip install --upgrade google-api-python-client google-auth-httplib2 google-auth-oauthlib

      - name: Set up Google Cloud credentials
        run: |
          echo "${{ secrets.GOOGLE_APPLICATION_CREDENTIALS }}" > $HOME/details.json
          cat $HOME/details.json  # Debugging step to ensure the file is correctly written

      - name: List directory contents
        run: ls -la $HOME  # Debugging step to ensure the file is in the correct directory

      - name: Run script
        run: python your_script.py
        env:
          GOOGLE_APPLICATION_CREDENTIALS: $HOME/details.json
